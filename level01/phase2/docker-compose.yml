services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    ports:
      - 7000:7000

  frontend:
    build: ./frontend
    ports:
      - 5173:5173
    command: npm run dev -- --host

  redis:
    image: redis
    ports:
      - 6379:6379